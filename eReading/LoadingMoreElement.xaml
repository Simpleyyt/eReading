<UserControl x:Class="eReading.LoadingMoreElement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="106" d:DesignWidth="572">
    <Border BorderBrush="{DynamicResource {x:Static SystemColors.AppWorkspaceBrushKey}}" Style="{DynamicResource BorderStyle1}">
    	<Border.Resources>
    		<Style x:Key="BorderStyle1" TargetType="{x:Type Border}">
    			<Style.Triggers>
    				<Trigger Property="IsMouseOver" Value="True">
    					<Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ActiveCaptionBrushKey}}"/>
    				</Trigger>
    			</Style.Triggers>
    			<Setter Property="Background" Value="White"/>
    		</Style>
    	</Border.Resources>
		<Grid>
			<Label Content="点击加载更多" x:Name="state" FontSize="14.667" HorizontalAlignment="Center" Width="Auto" Height="37" VerticalAlignment="Center" Foreground="{DynamicResource {x:Static SystemColors.HotTrackBrushKey}}" FontWeight="Bold" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" />
		</Grid>
	</Border>
</UserControl>
